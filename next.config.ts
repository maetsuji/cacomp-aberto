import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Nada de especial aqui: o projeto depende apenas de ISR nativo
  // (revalidatePath) e funções serverless padrão da Vercel.
};

export default nextConfig;
